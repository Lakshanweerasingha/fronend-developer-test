# fronend-developer-test

* clone the repository to your local computer.
* go to todo-app directory
* run the command 'npm install'
* after install, run 'npm run dev'
* then it will be open in browser
